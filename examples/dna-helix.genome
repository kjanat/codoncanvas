; DNA Double Helix - Visualization of DNA structure
; Demonstrates: LOOP, sinusoidal patterns, paired structures
; Biological concept: DNA structure, base pairing, helical geometry
; Difficulty: Advanced
; Instructions: 72 codons
; Author: CodonCanvas Autonomous Session 85

ATG
  ; Blue strand color
  GAA CGG GAA TTT GAA TGG TTA  ; COLOR (26, 85, 63)

  ; Left strand (backbone - vertical with wave)
  GAA TTA TCA                  ; PUSH 60(-4), LOOP (15 backbone segments)
    TGG                        ; SAVE_STATE
    ; Calculate sine wave offset (approximate with rotation)
    GAA GTC GAA AAA ACA        ; PUSH 45(-19), PUSH 0, TRANSLATE (wave left)
    GAA AAT GGA                ; PUSH 3, CIRCLE (backbone phosphate)
    TGT                        ; RESTORE_STATE
    GAA AAA GAA ACC ACA        ; PUSH 0, PUSH 5, TRANSLATE (0, 5) move down
  TCT

  ; Reset position for right strand
  GAA AAA GAA GTC ACA          ; PUSH 0, PUSH 45(-19), TRANSLATE (0, -300)

  ; Red strand color
  GAA AAA GAA TTT GAA TGG TTA  ; COLOR (0, 85, 63) red

  ; Right strand (backbone - vertical with opposite wave)
  GAA TTA TCA                  ; PUSH 60(-4), LOOP
    TGG                        ; SAVE_STATE
    GAA CCC GAA AAA ACA        ; PUSH 21, PUSH 0, TRANSLATE (wave right)
    GAA AAT GGA                ; PUSH 3, CIRCLE (backbone phosphate)
    TGT                        ; RESTORE_STATE
    GAA AAA GAA ACC ACA        ; PUSH 0, PUSH 5, TRANSLATE move down
  TCT

  ; Reset for base pairs
  GAA AAA GAA GTC ACA          ; PUSH 0, PUSH 45(-19), TRANSLATE back to top

  ; Yellow base pairs (connecting rungs)
  GAA TTT GAA TGT GAA TGG TTA  ; COLOR (63, 85, 63) yellow

  ; Draw base pairs (horizontal connectors)
  GAA CGG TCA                  ; PUSH 26, LOOP (10 base pairs)
    TGG                        ; SAVE_STATE
    GAA GTC GAA AAA ACA        ; PUSH 45(-19), PUSH 0, TRANSLATE left edge
    GAA GAC AGA                ; PUSH 33, ROTATE (horizontal)
    GAA TTA AAA                ; PUSH 60(-4), LINE (connect bases)
    TGT                        ; RESTORE_STATE
    GAA AAA GAA CGG ACA        ; PUSH 0, PUSH 26, TRANSLATE (0, down)
  TCT

TAA
